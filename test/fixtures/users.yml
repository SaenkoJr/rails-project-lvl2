DEFAULTS: &DEFAULTS
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

one:
  <<: *DEFAULTS
  email: <%= Faker::Internet.email %>

two:
  <<: *DEFAULTS
  email: <%= Faker::Internet.email %>
